{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "vercel-build": "npm install && vite build && curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-dev --prefer-dist --optimize-autoloader && rm composer.phar"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^5.1.4",
        "autoprefixer": "^10.4.24",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "postcss": "^8.5.6",
        "react": "^19.2.4",
        "react-dom": "^19.2.4",
        "tailwindcss": "^3.4.19",
        "vite": "^6.0.11"
    }
}
